__START__:
    MOVI R0, 10
    MOVI R1, 20
    ADD  R0, R1      ; R0 = R0 + R1

    PUSH R0
    MOV BP, SP       ; New frame

    CMP R0, R1
    JZ  EQUAL_LABEL

    MOVI R2, 0xFF
    POP R3

EQUAL_LABEL:
    MOV R4, BP
    MOV R5, SP
    MOV R6, PC
    MOV R7, IR
